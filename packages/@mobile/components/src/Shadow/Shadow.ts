import { ShadowedView } from "react-native-fast-shadow";

export { ShadowParams, shadowStyle } from "react-native-fast-shadow";

/**
 * Remember that shadow should be used to emphasise an element and not to style it visually.
 */
export const Shadow = ShadowedView;
