import { PagerContainer } from "./PagerContainer";
import { PagerControl } from "./PagerControl";
import { PagerPage } from "./PagerPage";
import { PagerRoot } from "./PagerRoot";

export const Pager = {
  Container: PagerContainer,
  Control: PagerControl,
  Page: PagerPage,
  Root: PagerRoot,
};

export * from "./Pager.types";
